# 距離重複候補の発走前比較 / 2026-09-08

## 状態

サブエージェントなしで実装。事前固定・結果照合・累積集計まで接続した。
本番の係数と点数、公開用データは変更していない。push未実施。
現在の実戦評価は0件。9/6の試算を発走前の予測として登録していない。

## 固定する方針

- 主比較: 能力と近走の両方から、今回の距離に対する直接の加減点を外す案。
- 参考比較: 能力側だけを外す案、近走側だけを外す案。結果を見て主比較を選び直さない。
- 3候補と現行の全馬点、同点首位、代表首位を同じ時刻に保存。同点の代表は馬番順。
- 距離適性点・総合配分は変更しない。近走加重平均修正やコース分離候補を混ぜない。
- 入力・モデル・予測のSHA256を保存。既存記録と入力やモデルが違えば上書きしない。
- 発走時刻以降の固定を拒否。発走時刻や馬場・距離などの整合性も検査。

## 成績の扱い

固定済みの首位だけを、馬番と馬名が一致する結果に照合する。
全馬の着順・単勝払戻・複勝払戻が揃わないレースは3候補とも除外し、理由を残す。
競走中止や取消なども現行の照合方式ではレース単位で除外するため、除外件数と併せて成績を読む。
欠損払戻を負けの0円に置き換えたり、欠けた首位を次点の馬に差し替えたりしない。

単勝・複勝それぞれ1レース100円均等で、勝数・複勝的中数・回収率を算出する。
首位入替があったレースだけの成績も分ける。両券種を合算した購入提案ではない。
累積は同日同一記録を1回だけ集計。同日で異なる記録はエラーにし、違うモデルの記録は別集計にする。
自動採用ゲートは追加していない。成績が良くても自動で本番へ接続しない。

## 実行方法

```powershell
node tools/analyze/evidence-overlap-shadow.mjs --freeze --input tools/week-data.next.json
node tools/analyze/evidence-overlap-shadow.mjs --evaluate data/shadow/direct-distance-overlap-v1/YYYY-MM-DD-pre-race.json --results data/archive/YYYY-MM-DD-results.json
node tools/analyze/evidence-overlap-shadow.mjs --aggregate
```

週次公開スクリプトに固定とステージの手順を追加した。今回スクリプト自体は実行していない。
レース後の結果照合と累積集計は上記コマンドで実行する。自動定期実行は今回追加していない。
日別成績はdocs/analysis/evidence-overlap-evaluation-日付.{json,md}、累積はevidence-overlap-cumulative.json。

## 検証

437件の回帰テスト成功。事前固定の時刻、3候補の完全性、代表首位の整合性、記録改変、
馬名不一致、結果欠損、同日の二重計上、モデル別集計、既存記録の上書き防止を検査した。
9/6のCLI事前固定は想定どおり失敗し、事前記録ファイルが作られていないことを確認。
データ整合性検査とViteビルドも成功。公開用tools/week-data.jsonは差分なし。
