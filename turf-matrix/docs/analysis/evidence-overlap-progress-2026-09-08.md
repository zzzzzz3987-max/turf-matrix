# 能力・近走・距離の重複診断 / 2026-09-08

## 完了範囲

サブエージェントは使用せず、計算経路の確認・全馬診断・調整候補の試算・回帰テストを実施。
公開用データ、本番の既定係数、既定の距離加点は変更していない。push未実施。

- 9/6の114頭すべてで保存済み能力・近走・距離点と総合指数を再現。
- 112頭で3項目に同じ過去走が選択されていた。延べ544馬別走、うち距離差200m以内435走。
- 能力内の相手関係が近走能力へフォールバックする馬は14頭。独立した実績の裏付けとは扱わない。
- 能力内の直接的な距離近似を外す候補: TM変更19頭、首位変更0レース。
- 近走内の距離加減点を外す候補: TM変更74頭、首位変更1レース。
- 両方を外す候補: TM変更84頭、差は-2〜+1点、首位変更1レース。

首位変更はセプテンバーS。現行のフクチャンショウ・カーヌスティ79点同点から、
両方除外の候補ではそれぞれ78点・79点になる。結果は参照していない。

## 実装上の注意

公開スナップショットには生成時のpeerRunsが出力されていない。
生成スクリプト内の同走馬照合を純粋関数として共通化し、保存済み過去走から同じ処理で復元した。
新しい外部の相手関係データを読んだり、保存済み能力点との差分を勝手に補正したりしない。
外部対戦相手情報が必要なのに固定済み入力がない場合、または元の指数が再現しない場合は試算を止める。

候補は能力・近走のincludeDistanceFitオプションで明示的に指定する。既定値はtrueのまま。
近走の加重平均修正、コースの芝ダート分離、総合配分の変更は今回の試算に混ぜていない。
全馬の利用過去走と候補差分はevidence-overlap-2026-09-06.json、読みやすい集計は同名Markdownに記録。

## 検証と残り

429テスト、データ整合性検査、Viteビルド、git diff --checkが成功。
同走馬復元、既定点の再現、他因子の保持、調教欠損、少走数補正、未来データ拒否をテストした。
人気・単勝オッズ・結果欄を変更しても、この診断の結果が変わらないことも確認。

重複利用だけで過大評価とは断定しない。着順・着差・上がりの共用を全て削る判断は保留。
今回の候補は感度診断用で、発走前記録・採用ゲート・週次自動公開には接続していない。
精度向上の判断には、候補を事前固定した独立比較が次に必要。
